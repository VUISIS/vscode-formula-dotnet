// Set Model
domain IntGraphs
{
	V ::= new (lbl: Integer).
	E ::= new (src: V, dst: V).
}

/*
  multiline
  comment
*/
model Gex of IntGraphs
{
	V(1).
	V(2).
	V(100).
	E(V(1), V(2)).
	E(V(100), V(100)).
}